# Python venvs and caches — never commit, regenerable via uv/pip
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.env

# Node deps and build output — never commit, regenerable via npm install/build
node_modules/
dist/
